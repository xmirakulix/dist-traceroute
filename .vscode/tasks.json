{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and run",
            "type": "shell",
            "command": "go build --tags 'json1' && sudo ./dist-traceroute test.at",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}