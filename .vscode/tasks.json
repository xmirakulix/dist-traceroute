{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and run",
            "type": "shell",
            "command": "go build cmd/gotraceroute.go && sudo ./gotraceroute test.at",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}